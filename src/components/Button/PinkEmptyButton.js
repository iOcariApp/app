import React from "react";
import { colors } from "theme";

import EmptyButton from "components/Button/EmptyButton";

const PinkEmptyButton = props => <EmptyButton color={colors.pink} {...props} />;

export default PinkEmptyButton;
