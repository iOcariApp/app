import React from "react";
import { colors } from "theme";

import Button from "./Button";

const PinkButton = props => <Button backgroundColor={colors.pink} {...props} />;

export default PinkButton;
