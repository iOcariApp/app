import React from "react";
import { colors } from "theme";

import Button from "./Button";

const BlueButton = props => <Button backgroundColor={colors.blue} {...props} />;

export default BlueButton;
